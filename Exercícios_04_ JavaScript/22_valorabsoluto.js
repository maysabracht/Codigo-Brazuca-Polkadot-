const number = -18; // Input number

function absoluteValue(number) {
    return Math.abs(number); // Calculate the absolute value and return it
}

console.log(`The absolute value is ${absoluteValue(number)}`); // Call the function and print the result